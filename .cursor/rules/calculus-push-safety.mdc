---
description: Public calculus_gacha — secrets only via .gitignore / local files
alwaysApply: true
---

# 公開リポジトリ

- 機密は **`.gitignore`**（`firebase_web_options` 実値、`GoogleService-Info.plist`、`google-services.json`、`key.properties` / keystore 等）。
- リポジトリに載せるのは **プレースホルダと `.example` のみ**。
